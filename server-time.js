(function(){'use strict'; Number.prototype.pad = function() {return this > 9 ? this : '0' + this; }; var $timeWrap = $('#time'), today = new Date(); today.setTime($timeWrap.data('timestamp') * 1000); function startTime() {today.setTime(today.getTime() + 1000); $timeWrap.html(today.getUTCHours().pad() + ":" + today.getUTCMinutes().pad() + ":" + today.getUTCSeconds().pad()); setTimeout(startTime, 1000); } startTime(); })($);